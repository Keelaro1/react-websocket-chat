import styled from 'styled-components';
import { ButtonStyled } from '../ui-kit/button/button.styled';

export const LocalizationButtonsStyled = styled.div`
	position: fixed;
	top: 10px;
	right: 10px;
	${ButtonStyled} {
		background: #a8acfd;
	}
`;
